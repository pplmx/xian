{
  // 示例是跑在 Node/Bun 上的命令行程序(要用 console),所以单独一份配置:
  // 库源码那份 `types: []` 刻意不引 Node 类型 —— 免得谁顺手在 src 里用了 process / Buffer
  // 还一路绿灯(库必须能在浏览器里跑)。
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"],
    "noEmit": true
  },
  "include": ["examples/**/*.ts"]
}
